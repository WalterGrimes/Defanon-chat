any (Props): Это первый параметр. Он отвечает за входящие свойства (props). Указав any, вы разрешаете передавать в этот компонент любые пропсы без проверки типов.

ChatState (State): Это второй параметр. Он строго определяет внутреннее состояние (state) компонента.